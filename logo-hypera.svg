/* ============================================================
   Hypera Pharma — Typography tokens
   Single family: Asap. Weights 400 / 500 / 600 / 700.
   H1/H2 = 36px/700. Body = 16–17px/400.
   ============================================================ */
:root {
  --font-sans: "Asap", system-ui, -apple-system, "Segoe UI", sans-serif;
  --font-icons: "Material Symbols Outlined";

  /* Weights */
  --fw-regular: 400; /* @kind other */
  --fw-medium: 500; /* @kind other */
  --fw-semibold: 600; /* @kind other */
  --fw-bold: 700; /* @kind other */

  /* Type scale */
  --fs-display: 56px;   /* hero / oversized */
  --fs-h1: 36px;
  --fs-h2: 36px;
  --fs-h3: 28px;
  --fs-h4: 22px;
  --fs-h5: 18px;
  --fs-h6: 16px;
  --fs-body-lg: 17px;   /* default paragraph on site */
  --fs-body: 16px;
  --fs-small: 14px;
  --fs-caption: 12px;

  /* Line heights */
  --lh-display: 1.1; /* @kind other */
  --lh-heading: 1.25; /* @kind other */   /* 36px → 45px */
  --lh-body: 1.45; /* @kind other */
  --lh-tight: 1.2; /* @kind other */

  /* Letter spacing */
  --ls-tight: -0.01em; /* @kind other */
  --ls-normal: 0; /* @kind other */
  --ls-wide: 0.02em; /* @kind other */
}
